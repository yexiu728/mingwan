package homework.day12.part1;

public class Test6 {
    /*

请简述HashSet去除重复元素的原理

先判断hashCode()是否相同，再判断equals()
    如果hashCode() 相同，则比较equals方法，如果equals也相同，则判断为相同元素，会将原值覆盖
    如果hashCode() 不同，则直接判断为不相同元素


*/
}
